-- fst: polymorphic
-- (++): polymorphic
-- not: not polymorphic
-- head: polymorphic
-- tail: polymorphic
-- id: polymorphic

-- Type Expression Exercises:
--exp1 :: ([String], Int)
--exp2 :: [a -> Bool]
--exp3 :: (String, String) -> [Bool]
--exp4 :: (Int -> Bool) -> Int
--exp5 :: (a -> a) -> (a -> a)

-- The zip function takes in two arbitrary lists
-- and packs the ith values together as pairs in a tuple.
--Example: zip ['x', 'y'] [False] is instantiated by a list of Char pairs to a, and a list of 
-- False booleans to b.

